meta {
  name: GET RIDE BY ID
  type: http
  seq: 3
}

get {
  url: {{API_URL}}/api/rides/{{RIDE_ID}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

vars:post-response {
  // Sauvegarder l'ID du ride créé pour les autres tests
  RIDE_ID: res.body.data.id
}

