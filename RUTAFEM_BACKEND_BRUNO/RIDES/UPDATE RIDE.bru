meta {
  name: UPDATE RIDE
  type: http
  seq: 4
}

patch {
  url: {{API_URL}}/api/rides/{{RIDE_ID}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{AUTH_TOKEN}}
}

body:json {
  {
      "price": 40,
      "availableSeats": 2
  }
}

settings {
  encodeUrl: true
}

