services:
  backend:
    build:
      context: ./src
      dockerfile: Dockerfile.dev
    ports:
      - 3000:3000
    volumes:
      - ./src:/app
      - /app/node_modules
    env_file:
      - ./src/.env
